[project]
name = "misongyny-dataset-paper"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ipywidgets>=8.1.9",
    "matplotlib>=3.11.1",
    "numpy>=2.5.2",
    "pandas>=3.0.5",
    "tqdm>=4.70.0",
    "transformers[torch]>=5.16.1",
]

[dependency-groups]
dev = [
    "nbconvert>=7.17.1",
]
